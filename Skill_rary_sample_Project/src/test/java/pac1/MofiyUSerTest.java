package pac1;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.testng.annotations.Test;

public class MofiyUSerTest {

	@Test(groups = "regressionTest")
	public void modifyUSerTest(){
		
		System.out.println("execute modify USer");
		/*
		 * WebDriver driver = new FirefoxDriver(); driver.get("http://localhost:8888/");
		 * 
		 * //login driver.findElement(By.name("user_name")).sendKeys("admin");
		 * driver.findElement(By.name("user_password")).sendKeys("manager");
		 * driver.findElement(By.id("submitButton")).click(); driver.close();
		 */
		
		

	}

}
